version: "3.8"
services:
  stad-spring:
    image: stad-spring-image
    container_name: stad-spring
    ports:
      - "18080:8080"
    networks:
      - ubuntu_my-network

networks:
  ubuntu_my-network:
    external: true
    name: ubuntu_my-network