spring:
  config:
    activate:
      on-profile: dev
  stad-spring:
    url: http://localhost:8080/api
server:
  port: 8081
---
spring:
  config:
    activate:
      on-profile: prod
  stad-spring:
    url: http://stad-spring:8080/api